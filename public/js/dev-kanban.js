/**
 * @deprecated Substituído por Stimulus (assets/controllers/*) + importmap core_projetos.
 * Mantido vazio para evitar 404 se algum cache antigo referenciar o arquivo.
 */
(function () {
    'use strict';
    if (typeof console !== 'undefined' && console.debug) {
        console.debug('[dev-kanban] use importmap core_projetos + Stimulus controllers.');
    }
})();
