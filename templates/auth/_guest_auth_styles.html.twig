<style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
        background: #07080F;
        font-family: 'Inter', sans-serif;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 24px;
    }
    .guest-auth-card {
        width: 100%;
        max-width: 420px;
        background: #0C0D18;
        border: 1px solid rgba(255,255,255,0.07);
        border-radius: 16px;
        padding: 40px 36px;
    }
    .brand-name {
        font-family: 'Sora', 'Inter', sans-serif;
        font-size: 28px;
        font-weight: 700;
        color: #fff;
        letter-spacing: -0.04em;
        margin-bottom: 24px;
    }
    .brand-name em { font-style: normal; color: #6B9AFF; }
    h1 { color: #fff; font-size: 20px; font-weight: 700; margin-bottom: 6px; }
    .sub { color: rgba(255,255,255,0.35); font-size: 13px; line-height: 1.5; margin-bottom: 24px; }
    .form-group { margin-bottom: 14px; }
    .form-label {
        color: rgba(255,255,255,0.38);
        font-size: 10px;
        font-weight: 600;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        display: block;
        margin-bottom: 5px;
    }
    .form-control {
        background: rgba(255,255,255,0.04);
        border: 1px solid rgba(255,255,255,0.08);
        border-radius: 7px;
        color: #fff;
        padding: 10px 13px;
        font-size: 13px;
        width: 100%;
        outline: none;
    }
    .form-control:focus {
        border-color: rgba(61,114,255,0.5);
        box-shadow: 0 0 0 3px rgba(61,114,255,0.1);
    }
    .invalid-feedback { color: #f87171; font-size: 11px; margin-top: 4px; display: block; }
    .form-row-cols { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
    .btn-primary {
        width: 100%;
        background: #3D72FF;
        color: #fff;
        border: none;
        border-radius: 7px;
        padding: 12px;
        font-size: 13px;
        font-weight: 600;
        cursor: pointer;
        margin-top: 4px;
    }
    .btn-primary:hover { background: #5585ff; }
    .guest-auth-back { text-align: center; margin-top: 18px; font-size: 12px; }
    .guest-auth-back a { color: #6B9AFF; text-decoration: none; }
    @media (max-width: 480px) { .form-row-cols { grid-template-columns: 1fr; } }
</style>
