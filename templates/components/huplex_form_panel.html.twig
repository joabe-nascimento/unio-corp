{#
  huplex_form_panel — card de formulário padronizado

  Props:
    title     (string, opt)
    subtitle  (string, opt)
    max_width (int, opt)  px, default 640

  Blocks:
    form_panel_fields
    form_panel_actions
#}
{% set panel_max_width = max_width is defined ? max_width : 640 %}
<div class="huplex-card huplex-form-panel" style="max-width: {{ panel_max_width }}px;">
    {% if title is defined and title %}
        <div class="section-title mb-1">{{ title }}</div>
    {% endif %}
    {% if subtitle is defined and subtitle %}
        <p class="section-subtitle mb-4">{{ subtitle }}</p>
    {% elseif title is defined and title %}
        <div class="mb-4"></div>
    {% endif %}
    <form method="post" class="huplex-form">
        {% block form_panel_fields %}{% endblock %}
        <div class="huplex-form-actions">
            {% block form_panel_actions %}
                <button type="submit" class="btn-huplex">{{ submit_label is defined ? submit_label : 'Salvar' }}</button>
            {% endblock %}
        </div>
    </form>
</div>
