{# Página placeholder de submódulo #}
<div class="huplex-card">
    <div class="section-title mb-1">{{ page_title }}</div>
    <div class="section-subtitle mb-4">{{ page_subtitle|default('Em construção') }}</div>
    {% include 'components/empty_state.html.twig' with {
        icon: empty_icon|default('fa-tools'),
        title: empty_title|default('Módulo em desenvolvimento'),
        text: empty_text|default('Esta funcionalidade estará disponível em breve.')
    } %}
</div>
