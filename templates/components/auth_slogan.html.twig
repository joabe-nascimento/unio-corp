{# Slogan institucional — versão por tema (painel esquerdo do login) #}
<div class="auth-slogan" role="img" aria-label="Pessoas, processos e resultados">
    <img class="auth-slogan__img auth-slogan__img--dark"
         src="{{ asset('images/auth/slogan-branco.png') }}?v=2"
         alt=""
         width="640"
         height="48"
         decoding="async">
    <img class="auth-slogan__img auth-slogan__img--light"
         src="{{ asset('images/auth/slogan-preto.png') }}?v=2"
         alt=""
         width="640"
         height="48"
         decoding="async">
</div>
