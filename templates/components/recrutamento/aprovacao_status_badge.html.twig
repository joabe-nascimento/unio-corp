{#
  recrutamento/aprovacao_status_badge — status de solicitação de aprovação

  Props:
    aprovacao   (RhCandidatoAprovacao, required)
    extra_class (string, opt)
    attrs       (object, opt)
#}
{% include 'components/status_badge.html.twig' with {
    label: aprovacao.statusLabel,
    variant: aprovacao.statusClass,
    extra_class: ('ti-badge-health ' ~ extra_class|default(''))|trim,
    attrs: attrs|default({})
} %}
