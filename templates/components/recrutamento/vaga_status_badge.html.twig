{#
  recrutamento/vaga_status_badge — status da vaga

  Props:
    vaga        (RhVaga, required)
    extra_class (string, opt)
    attrs       (object, opt)
#}
{% include 'components/status_badge.html.twig' with {
    label: vaga.statusLabel,
    variant: vaga.statusClass,
    extra_class: ('ti-badge-health ' ~ extra_class|default(''))|trim,
    attrs: attrs|default({})
} %}
