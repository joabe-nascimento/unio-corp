{#
  recrutamento/candidato_etapa_badge — etapa do candidato no funil

  Props:
    candidato   (RhCandidato, required)
    extra_class (string, opt)
    attrs       (object, opt)
#}
{% include 'components/status_badge.html.twig' with {
    label: candidato.etapaLabel,
    variant: candidato.etapaClass,
    extra_class: ('ti-badge-health ' ~ extra_class|default(''))|trim,
    attrs: attrs|default({})
} %}
