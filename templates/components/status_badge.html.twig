{#
  status_badge — badge de status (substitui badge Bootstrap)

  Props:
    label   (string)
    variant (string)  info | success | warning | secondary  (default: secondary)
#}
<span class="status-badge status-badge--{{ variant|default('secondary') }}">{{ label }}</span>
