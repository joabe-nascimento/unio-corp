{#
  Hub em desenvolvimento — empty state com ícone grande

  Props:
    icon   (string)
    title  (string)
    text   (string, opt)
#}
<div class="empty-state-panel">
    {% include 'components/empty_state.html.twig' with {
        icon: icon|default('fa-layer-group'),
        title: title|default('Em desenvolvimento'),
        text: text|default('Este hub estará disponível em breve. Estamos preparando novas funcionalidades para você.'),
        hero: true
    } %}
</div>
