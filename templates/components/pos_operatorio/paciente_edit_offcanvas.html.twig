{% embed 'components/unio_offcanvas.html.twig' with {
    id: 'pos-op-paciente-edit',
    title: 'Editar paciente',
    subtitle: 'Atualize o cadastro clínico',
    icon: 'fa-pen',
    size: 'xl'
} %}
    {% block offcanvas_body %}
        <p class="unio-offcanvas-context text-muted small mb-3" id="posOpPacienteEditLabel"></p>
        <div id="posOpPacienteEditHost" data-pos-op-form-partial-url="{{ path('app_pos_operatorio_paciente_form_partial', { id: 0 }) }}">
            <p class="text-muted small mb-0">Carregando formulário…</p>
        </div>
    {% endblock %}
{% endembed %}
