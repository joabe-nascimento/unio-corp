{% extends 'layout/hub.html.twig' %}

{% block title %}Qualidade · {{ org_brand_label|default('Unio Saúde') }}{% endblock %}

{% set hub_page_title = 'Qualidade clínica' %}
{% set hub_page_eyebrow = org_brand_label %}
{% set hub_page_subtitle = 'Taxa de resposta, SLA e distribuição por dia pós-operatório' %}
{% set hub_search_hidden = true %}

{% block hub_kpis %}
{% include 'components/kpi_row.html.twig' with { items: [
    { value: quality.taxa_resposta ~ '%', label: 'Taxa resposta hoje', icon: 'fa-percent', tone: 'sage' },
    { value: quality.pendentes_hoje, label: 'Pendentes hoje', icon: 'fa-clock', tone: 'amber' },
    { value: quality.sla_pct ~ '%', label: 'SLA em dia', icon: 'fa-stopwatch', tone: 'lavender' },
    { value: quality.pacientes_ativos, label: 'Pacientes ativos', icon: 'fa-user-injured', tone: 'sky' },
], mb: 'mb-0' } %}
{% endblock %}

{% block hub_content %}
<div class="clinic-ops-panels clinic-ops-panels--3">
    <section class="unio-card clinic-ops-panel p-3">
        <h3 class="clinic-ops-panel__title">Questionários hoje</h3>
        <dl class="clinic-ops-stats">
            <div><dt>Respondidos</dt><dd>{{ quality.respondidos_hoje }}</dd></div>
            <div><dt>Pendentes</dt><dd>{{ quality.pendentes_hoje }}</dd></div>
            <div><dt>Alertas abertos</dt><dd>{{ quality.alertas_abertos }}</dd></div>
        </dl>
    </section>

    <section class="unio-card clinic-ops-panel p-3">
        <h3 class="clinic-ops-panel__title">Pacientes por fase</h3>
        {% if quality.heatmap is empty %}
        <p class="text-muted small mb-0">Sem dados de acompanhamento.</p>
        {% else %}
        <ul class="clinic-ops-list">
            {% for bucket, count in quality.heatmap %}
            <li><strong>{{ bucket }}</strong> — {{ count }} paciente(s)</li>
            {% endfor %}
        </ul>
        {% endif %}
    </section>

    <section class="unio-card clinic-ops-panel p-3">
        <h3 class="clinic-ops-panel__title">Ações</h3>
        <div class="d-flex flex-column gap-2">
            <a href="{{ path('app_pos_operatorio_questionarios') }}" class="btn-unio-ghost btn-sm">Ver questionários</a>
            <a href="{{ path('app_pos_operatorio_alertas') }}" class="btn-unio-ghost btn-sm">Fila de alertas</a>
            <a href="{{ path('app_pos_operatorio_relatorios') }}" class="btn-unio-ghost btn-sm">Relatórios</a>
        </div>
    </section>
</div>
{% endblock %}
