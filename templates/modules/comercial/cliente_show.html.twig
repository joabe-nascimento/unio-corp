{% extends 'layout/hub.html.twig' %}
{% block title %}{{ conta.nome }} · Cliente{% endblock %}
{% block permissions_scope %}hub_comercial{% endblock %}
{% set hub_page_title = conta.nome %}
{% set hub_page_subtitle = 'Cliente · ' ~ conta.status %}

{% block hub_toolbar_actions %}
<a href="{{ path('app_comercial_clientes') }}" class="btn-unio-ghost btn-sm">Voltar</a>
{% endblock %}

{% block hub_content %}
<div class="row g-4">
    <div class="col-lg-8">
        <div class="unio-card">
            {% include 'components/section_header.html.twig' with { title: 'Editar cliente' } %}
            <form method="post" class="px-3 pb-3">
                <input type="hidden" name="_token" value="{{ csrf_token('crm_conta_edit_' ~ conta.id) }}">
                <div class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label" for="nome">Nome *</label>
                        <input class="form-control" id="nome" name="nome" value="{{ conta.nome }}" required>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label" for="documento">CNPJ/CPF</label>
                        <input class="form-control" id="documento" name="documento" value="{{ conta.documento }}">
                    </div>
                    <div class="col-md-4">
                        <label class="form-label" for="email">E-mail</label>
                        <input class="form-control" id="email" type="email" name="email" value="{{ conta.email }}">
                    </div>
                    <div class="col-md-4">
                        <label class="form-label" for="telefone">Telefone</label>
                        <input class="form-control" id="telefone" name="telefone" value="{{ conta.telefone }}">
                    </div>
                    <div class="col-md-4">
                        <label class="form-label" for="segmento">Segmento</label>
                        <input class="form-control" id="segmento" name="segmento" value="{{ conta.segmento }}">
                    </div>
                    <div class="col-md-6">
                        <label class="form-label" for="site">Site</label>
                        <input class="form-control" id="site" name="site" value="{{ conta.site }}">
                    </div>
                    <div class="col-md-6">
                        <label class="form-label" for="status">Status</label>
                        <select class="form-control" id="status" name="status">
                            {% for s in status_options %}
                            <option value="{{ s }}" {% if conta.status == s %}selected{% endif %}>{{ s }}</option>
                            {% endfor %}
                        </select>
                    </div>
                    <div class="col-12">
                        <label class="form-label" for="notas">Notas</label>
                        <textarea class="form-control" id="notas" name="notas" rows="3">{{ conta.notas }}</textarea>
                    </div>
                    <div class="col-12">
                        <button type="submit" class="btn-unio btn-sm">Salvar</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
    <div class="col-lg-4">
        <div class="unio-card">
            {% include 'components/section_header.html.twig' with { title: 'Ações' } %}
            <div class="px-3 pb-3 d-grid gap-2">
                <a href="{{ path('app_comercial_pipeline', { novo: 1 }) }}" class="btn-unio-ghost btn-sm">Nova oportunidade</a>
                <form method="post" action="{{ path('app_comercial_cliente_excluir', { id: conta.id }) }}" onsubmit="return confirm('Excluir cliente?');">
                    <input type="hidden" name="_token" value="{{ csrf_token('crm_conta_delete_' ~ conta.id) }}">
                    <button type="submit" class="btn-unio-ghost btn-sm text-danger">Excluir cliente</button>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}
