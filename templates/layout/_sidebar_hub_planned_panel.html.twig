<li class="nav-item sidebar-hub-current">
    <a href="{{ path(hub.route) }}" class="nav-link{{ route == hub.route ? ' active' : '' }}">
        <i class="fas {{ hub.icon }} nav-icon"></i>
        <p>{{ hub.label }}</p>
    </a>
</li>
