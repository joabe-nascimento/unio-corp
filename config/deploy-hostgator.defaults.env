# HostGator — valores padrao versionados (nao sao secrets).
# SSH deve usar o hostname do servidor cPanel, NUNCA dominio publico (Cloudflare).
DEPLOY_SSH_CANONICAL_HOST=br1136.hostgator.com.br
DEPLOY_SSH_DEFAULT_USER=joabef36
DEPLOY_SSH_DEFAULT_PORT=2222
