# HostGator: stderr do PHP nem sempre aparece no cPanel — gravar também em arquivo.
when@prod:
    monolog:
        handlers:
            file:
                type: stream
                path: "%kernel.logs_dir%/prod.log"
                level: debug
                channels: ["!event"]
