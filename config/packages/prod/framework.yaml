# Produção: sessão fora de var/cache (cache:clear não derruba login).
framework:
    session:
        save_path: '%kernel.project_dir%/var/sessions'
        gc_maxlifetime: 7200
        cookie_lifetime: 0
        cookie_secure: auto
        cookie_samesite: lax
        cookie_httponly: true
