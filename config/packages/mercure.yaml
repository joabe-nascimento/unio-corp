mercure:
    hubs:
        default:
            url: '%env(default:mercure_url_default:MERCURE_URL)%'
            public_url: '%env(default:mercure_url_default:MERCURE_PUBLIC_URL)%'
            jwt:
                secret: '%env(default:mercure_jwt_default:MERCURE_JWT_SECRET)%'
                publish: '*'
